import React from 'react';

export default function PeriodAndCalculations() {
  return (
    <div>
      <label htmlFor="cars">Escolha um periodo:</label>
      <select id="period" name="period">
        <option value="volvo">Volvo</option>
      </select>
    </div>
  );
}
